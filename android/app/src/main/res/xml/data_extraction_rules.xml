<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing is backed up or transferred to a new device: the local database holds
  SMS content and the encrypted prefs hold device tokens. Both must stay on the
  phone that was explicitly authorized. Re-verify by OTP on a new device instead.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
