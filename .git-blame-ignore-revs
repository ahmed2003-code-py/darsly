# Revisions that changed formatting only. `git blame` skips them so it keeps
# pointing at the commit that actually wrote each line.
#
# Enable locally once:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub reads this file automatically.

# style: format the repository with Prettier (530 files, whitespace only)
afc76926a1c3c2c824f2d1efd700b45411511a35
